__pycache__/
*.pyc
.env
venv/
.venv/
node_modules/
dist/
data/
*.db

# Local firmware copy with real credentials — never push.
# Only the sanitized garage_door.ino.example is tracked.
esp32/garage_door/garage_door.ino

# Arduino build artifacts (Export Compiled Binary output)
esp32/garage_door/build/
